<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Songs</title>
  </head>
  <body>
    <h1>All Songs</h1>
    <ul>
      <% Song.all.each do |song|%>
      <li><a href="/songs/<%=song.slug%>"><%= song.name %></a></li>
      <% end %>
    </ul>
  </body>
</html>
