<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Song</title>
  </head>
  <body>
    <% if flash.has?(:message) %>
    <%= flash[:message] %>
    <% end %>
    <h1>Song Name: <%=@song.name %> </h1>
    <h1>Artist: <a href="/artists/<%=@song.artist.slug%>"><%=@song.artist.name %></a></h1>
    <h1>Genres: </h1><br>
    <ul>
      <% @song.genres.each do |genre| %>
      <li> <a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
      <% end %>
    </ul>

  </body>
</html>
